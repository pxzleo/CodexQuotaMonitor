<Window x:Class="CodexQuotaMonitor.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:CodexQuotaMonitor.Wpf"
        Title="Codex Quota Monitor Native"
        Width="260"
        Height="48"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Icon="Assets/CodexQuotaMonitor.ico"
        Background="#01FFFFFF"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="ClearType"
        Loaded="OnLoaded"
        SourceInitialized="OnSourceInitialized"
        Closing="OnClosing"
        MouseRightButtonUp="OnMouseRightButtonUp"
        MouseLeftButtonUp="OnMouseLeftButtonUp">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="8"
                            GlassFrameThickness="-1"
                            ResizeBorderThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="OuterBorder"
            Margin="1"
            CornerRadius="8"
            Background="#E6202428"
            BorderBrush="#70FFFFFF"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition x:Name="CurveRowDefinition" Height="0" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Margin="1"
                    CornerRadius="7"
                    BorderBrush="#2EFFFFFF"
                    BorderThickness="1,1,0,0"
                    Grid.RowSpan="2" />
            <Border x:Name="CurvePanelHost"
                    Grid.Row="0"
                    Margin="3,2,3,0"
                    Visibility="Collapsed">
                <local:QuotaTrendBlock x:Name="CurvePanel" />
            </Border>
            <Grid x:Name="RootGrid" Grid.Row="1" Margin="3,2,3,2" />
        </Grid>
    </Border>
</Window>
